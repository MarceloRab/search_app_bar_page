name: search_app_bar_page
description: A search page built for minimal work and good performance. Enjoy.
version: 1.6.0
homepage: https://github.com/MarceloRab/search_app_bar_page

environment:
  sdk: ">=2.9.0 <3.0.0"
  #flutter: ">=1.17.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter
  diacritic: ">=0.1.3 <2.0.0" # mudar letras - acentuação
  get: ">=3.26.0 <4.0.0"
  meta: ^1.2.4
  #connectivity: ">=2.0.2 <3.0.0"
  connectivity: ">=3.0.2 <4.0.0"


dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.3.0


flutter:

